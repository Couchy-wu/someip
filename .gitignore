# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.pipcache/
.testdeps/

# Docker 本地配置（由 run_tests.sh 生成）
.dockerconfig/

# 临时/构建产物
.dist/

# to_longjie x86 SP 库（76MB，zip 内已含，需要时解压）
to_longjie_demo_20250625/libs/lib_x86/

# arhud_python_server 构建产物
arhud_python_server/out/

# x86_64 SOME/IP 协议栈库约 64MB，不入库（获取方式见 arhud_python_server/libs/README.md）
arhud_python_server/libs/x86_64/*.so

# 服务端库编译产物（源码见 arhud_python_server/src，库按需就地编译）
arhud_python_server/src/*.o
arhud_python_server/src/*.so
