{
  "meta": {
    "purpose": "Di 用例里有些报文只写了“门控信号有效”却没给位域（497 个样例中 403 条），无法据此编码。若已从 CAN 矩阵确认这些门控位，在此按报文补齐，执行器会在合成帧时套用。",
    "format": "{\"<CAN ID 十六进制，如 0x4C1>\": {\"<位域 base=0，如 6.2-6.3>\": <值>}}",
    "note": "留空表示不套用任何默认位；缺位域的报文会被如实记为“无法编码”，不会被当成已下发。"
  },
  "defaults": {}
}
