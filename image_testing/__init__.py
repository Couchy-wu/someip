# -*- coding: utf-8 -*-
"""image_testing —— 图标测试：图像相似度、图标库管理、测试图生成与校验

纯图像算法与数据模型；不依赖 gui 包。

依赖约束：上层（gui_handlers / can_gui / main）可依赖本包；
          本包不反向依赖界面层（保持可测试、可复用）。
说明：本文件只声明包边界与职责，不在导入时引入重依赖（无副作用）。
"""
